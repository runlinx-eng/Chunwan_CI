# Theme Precision Gate Notes

- Threshold evolution: tighten gates first, stabilize, then update the baseline; commit the baseline update separately.
- Anti-constant ratio: `unique_value_ratio` uses the `theme_total` sample count (result rows/items), not positive counts or signal-level expansions.
